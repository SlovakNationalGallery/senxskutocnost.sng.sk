{% embed 'partials/base.html.twig' %}

{% block body %}
<body itemscope itemtype="http://schema.org/Article">
{% endblock %}

{% block content %}
    <main class="landing {{ page.header.image ? 'tag-articleimage' : '' }}" role="main">
        <div class="landing-image">
          <div class="landing-image-image" style="background-image: url({% if page.header.image %}{{ page.media[page.header.image].url }}{% endif %})"></div>
          <div class="landing-image-fade"></div>
        </div> {# /div.landing-image #}

        <div class="landing-content">
          <div class="centered uk-container-center uk-width-9-10 uk-width-small-8-10">
            <div id="landing-titles">
              <h1 class="c-white"><span class="f-uni-grotesk-bold">sen</span> <img src="{{ theme_url }}/images/headline-logo.svg" alt="x" class="headline-logo"> <span class="f-moyenage">skutočnosť</span></h1>
              <h2 class="c-white f-uni-grotesk-bold f-uppercase">{{ page.header.h2 }}</h2>
              <h3 class="c-white f-uni-grotesk-regular">{{ page.header.h3 }}</h3>
            </div>

            {% include 'partials/emailcapture.html.twig' with {
              'emailCTA': page.header.emailCTA, 
              'emailPlaceholder': page.header.emailPlaceholder, 
              'emailButton': page.header.emailButton, 
              'emailNotice': page.header.emailNotice} 
            %}
            <div>
              {% include 'partials/sharing.html.twig' %}
            </div>

          </div>
          
          <div id="chapter-preview" class="uk-grid centered c-white ">

            <div class="uk-width-1-1 uk-width-medium-1-2 uk-width-large-1-4 chapter-thumb {% if page.find('/chapters/1').header.disabled%}disabled{% endif %}">
              <a href="{{page.find('/chapters/1').url}}" {% if page.find('/chapters/1').header.disabled%}disabled{% endif %}>
                <h5 class="f-uni-grotesk-bold">{{page.find('/chapters/1').header.chapterNumber}}.<br>{{page.find('/chapters/1').title}}</h5>
              </a>
              <p class="f-uni-grotesk-regular">{{page.find('/chapters/1').header.announce_date}}</p>
              <a href="{{page.find('/chapters/1').url}}">
                <img src="
                {% if page.find('/chapters/1').header.disabled %}
                  {{ page.media['thumb-chapter1.jpg'].colorize(-100,-100,-100).url }}
                {% else %}
                  {{ page.media['thumb-chapter1.jpg'].url }}
                {% endif %}"
                class="uk-hidden-small" alt="sen x skutočnosť {{page.find('/chapters/1').header.chapterNumber}}. kapitola: {{page.find('/chapters/1').title}}">
              </a>
            </div>

            <div class="uk-width-1-1 uk-width-medium-1-2 uk-width-large-1-4 chapter-thumb {% if page.find('/chapters/2').header.disabled%}disabled{% endif %}">
              <a href="{{page.find('/chapters/2').url}}" {% if page.find('/chapters/2').header.disabled%}disabled{% endif %}>
                <h5 class="f-uni-grotesk-bold">{{page.find('/chapters/2').header.chapterNumber}}.<br>{{page.find('/chapters/2').title}}</h5>
              </a>
              <p class="f-uni-grotesk-regular">{{page.find('/chapters/2').header.announce_date}}</p>
              <img src="
                {% if page.find('/chapters/2').header.disabled %}
                  {{ page.media['thumb-chapter2.jpg'].colorize(-100,-100,-100).url }}
                {% else %}
                  {{ page.media['thumb-chapter2.jpg'].url }}
                {% endif %}" 
                class="uk-hidden-small" alt="sen x skutočnosť {{page.find('/chapters/2').header.chapterNumber}}. kapitola: {{page.find('/chapters/2').title}}">
            </div>

            <div class="uk-width-1-1 uk-width-medium-1-2 uk-width-large-1-4 chapter-thumb {% if page.find('/chapters/3').header.disabled%}disabled{% endif %}">
              <a href="{{page.find('/chapters/3').url}}" {% if page.find('/chapters/3').header.disabled%}disabled{% endif %}>
                <h5 class="f-uni-grotesk-bold">{{page.find('/chapters/3').header.chapterNumber}}.<br>{{page.find('/chapters/3').title}}</h5>
              </a>
              <p class="f-uni-grotesk-regular">{{page.find('/chapters/3').header.announce_date}}</p>
              <img src="
                {% if page.find('/chapters/3').header.disabled %}
                  {{ page.media['thumb-chapter3.jpg'].colorize(-100,-100,-100).url }}
                {% else %}
                  {{ page.media['thumb-chapter3.jpg'].url }}
                {% endif %}"
                class="uk-hidden-small" alt="sen x skutočnosť {{page.find('/chapters/3').header.chapterNumber}}. kapitola: {{page.find('/chapters/3').title}}">
            </div>

            <div class="uk-width-1-1 uk-width-medium-1-2 uk-width-large-1-4 chapter-thumb {% if page.find('/chapters/4').header.disabled%}disabled{% endif %}">
              <a href="{{page.find('/chapters/4').url}}" {% if page.find('/chapters/4').header.disabled%}disabled{% endif %}>
                <h5 class="f-uni-grotesk-bold">{{page.find('/chapters/4').header.chapterNumber}}.<br>{{page.find('/chapters/4').title}}</h5>
              </a>
              <p class="f-uni-grotesk-regular">{{page.find('/chapters/4').header.announce_date}}</p>
              <img src="
                {% if page.find('/chapters/4').header.disabled %}
                  {{ page.media['thumb-chapter4.jpg'].colorize(-100,-100,-100).url }}
                {% else %}
                  {{ page.media['thumb-chapter4.jpg'].url }}
                {% endif %}"
                class="uk-hidden-small" alt="sen x skutočnosť {{page.find('/chapters/4').header.chapterNumber}}. kapitola: {{page.find('/chapters/4').title}}">
            </div>

          </div>
          
        </div>

        {% include 'partials/footer.html.twig' with {
          'sharingButtons': false,
          'exhibitionLink': true,
          'orgIcons':       true,
          'copyright':      true,
          'stylingClasses': 'c-grey'}
        %}
        
    </main>
    
{% endblock %}

{% block footer %}{% endblock %}

{% endembed %}
